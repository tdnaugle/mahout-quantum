import numpy as np 

# initialize basis state as numpy array
def create_np_basis_state(qubit_count: int = 1,
                          data: tuple) -> np.array:
    return None

# initialize bell state (as tuple of numpy array?)